kind: Service
apiVersion: v1
metadata:
  name: mcp-server-knowledge
  namespace: aegis
spec:
  selector:
    app: mcp-server-knowledge
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: knowledge-mcp
  namespace: aegis
  labels:
    k8s-app: knowledge-mcp
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - knowledge.af.atlas-ml.org
      secretName: letsencrypt-generated-knowledge-mcp
  rules:
    - host: knowledge.af.atlas-ml.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mcp-server-knowledge
                port:
                  number: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server-knowledge
  namespace: aegis
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-server-knowledge
  template:
    metadata:
      labels:
        app: mcp-server-knowledge
        role: mcp-server
    spec:
      serviceAccountName: mcp-server-knowledge
      automountServiceAccountToken: false
      containers:
        - name: mcp-server-knowledge
          # image: hub.opensciencegrid.org/maniaclab/mcp-server-knowledge
          image: harbor.af.uchicago.edu/maniaclab/mcp-server-knowledge
          ports:
            - containerPort: 3001
          envFrom:
            - secretRef:
                name: mcp-keys
            - configMapRef:
                name: keycloak-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mcp-server-knowledge
  namespace: aegis
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mcp-server-knowledge
