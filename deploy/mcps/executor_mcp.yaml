kind: Service
apiVersion: v1
metadata:
  name: mcp-server-executor
  namespace: aegis
spec:
  selector:
    app: mcp-server-executor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: executor-mcp
  namespace: aegis
  labels:
    k8s-app: executor-mcp
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - executor.af.atlas-ml.org
      secretName: letsencrypt-generated-executor-mcp
  rules:
    - host: executor.af.atlas-ml.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mcp-server-executor
                port:
                  number: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server-executor
  namespace: aegis
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-server-executor
  template:
    metadata:
      labels:
        app: mcp-server-executor
        role: mcp-server
    spec:
      serviceAccountName: mcp-server-executor
      automountServiceAccountToken: false
      containers:
        - name: mcp-server-executor
          # image: hub.opensciencegrid.org/maniaclab/mcp-server-executor
          image: harbor.af.uchicago.edu/maniaclab/mcp-server-executor
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: af-ssh-key
            - secretRef:
                name: mcp-keys
            - configMapRef:
                name: keycloak-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mcp-server-executor
  namespace: aegis
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mcp-server-executor
