# Executor MCP

it runs bash commands and scripts.
Has two tools:

* returns json
* returns string

*Transport Type:* Streamable HTTP
*URL:* <http://localhost:3200/mcp>

inspect
    npx run inspector

start
    npx run start
